# Runtime artifacts — see README "State artifacts" (not source)
state/
watch/specs/
KILL
*.log
*.pid

# Re-include committed test fixtures (root .gitignore has a global *.json rule)
!tests/fixtures/*.json

# console config backup (runtime artifact)
config.yaml.bak

# PocketBase runtime install (33MB binary + pb_data + pb.env secrets)
.pocketbase/
