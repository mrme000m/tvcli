# Secrets & local state
.env
.env.bak
authenticated_cookies.json
.tv-meta.json
.tv-scripts/

# Compiled binaries
/tv
/tvcli
/tvcli-linux
/tvcli-arm64
/tvcli.exe

# OS
.DS_Store

# Tool caches
__pycache__/
*.pyc
.mimocode/
.crush/

# Editor / agent tool integration (not part of the Go project)
.agents/*
!.agents/skills/
!.agents/agents/
!.agents/workflows/
.claude/
.codex/
.opencode/
.openknowledge/
.pi/
opencode.json

# Open Knowledge scaffold (auto-generated, not part of Go project)
Wiki/

# Temp data output (fetch, runs, analysis)
rawdumps/
skill-runs/
skill-analysis/
.tv-src/
top_scripts.json

# NotebookLM extracts
notebooklm_*.json

# One-off scripts & session artifacts
run_all_skills.sh
pi-session-*.html
CONSOLIDATED_SCALP_SCRIPT.md

# Fetch output files (CSV/JSON at root)
*.csv
BINANCE_*.json

# editor backups
*.bak

# working artifacts from agent runs
skill_work/
*.json
!.devcontainer/devcontainer.json

# Background server state
.tvcli-server.pid
.tvcli-server.log

# Local build artifacts / screenshots
/bulk_validate
/shots/
accounts.json.bak*

# bw-provision runtime secrets (never commit; values live in Bitwarden)
browser-debug/secrets/runtime/
.tvcli-autoserve
!browser-debug/secrets/manifest.json

# vendored tvvisual (visual/) — recipes + example are source, not artifacts
!visual/recipes/*.json
!visual/chart.example.json
